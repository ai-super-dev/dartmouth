{
  "name": "agent-army-system",
  "version": "1.0.0",
  "description": "Modular AI agent platform with foundational base and specialized agents",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/worker",
    "build": "npm run build --workspaces",
    "build:deps": "npm run build --workspace=packages/shared && npm run build --workspace=packages/integration-services",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "deploy": "npm run build:deps && npm run deploy --workspace=packages/worker"
  },
  "keywords": [
    "ai",
    "agent",
    "chatbot",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}

