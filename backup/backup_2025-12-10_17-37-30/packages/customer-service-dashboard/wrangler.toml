# Wrangler configuration for Customer Service Dashboard (Cloudflare Pages)

name = "customer-service-dashboard"
compatibility_date = "2024-11-01"

pages_build_output_dir = "dist"

# Environment variables for the Pages project
[env.production]
# Add any environment variables needed for the dashboard here

